# GitHub Pages 部署指南

## 📋 部署步骤

### 第一步：创建 GitHub 仓库

1. 登录 GitHub
2. 点击右上角的 `+` 号，选择 `New repository`
3. 填写仓库信息：
    - **Repository name**: `popup-windows` (或你喜欢的名字)
    - **Description**: `温馨弹窗 - Web版`
    - **Visibility**: 选择 `Public` (GitHub Pages 免费版需要公开仓库)
    - **不要**勾选 "Initialize this repository with a README"
4. 点击 `Create repository`

### 第二步：上传文件到 GitHub

#### 方法 1：使用 GitHub 网页上传（最简单）

1. 在刚创建的仓库页面，点击 `uploading an existing file`
2. 将 `web_version` 文件夹中的所有文件拖拽上传：
    - `index.html`
    - `style.css`
    - `script.js`
    - `README.md`
3. 在页面底部填写提交信息：`Initial commit`
4. 点击 `Commit changes`

#### 方法 2：使用 Git 命令行（推荐）

在终端中运行以下命令：

```bash
# 进入项目目录
cd /Users/aaron/xinxin/弹窗/web_version

# 初始化 Git 仓库
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit: 温馨弹窗 Web版"

# 添加远程仓库（替换 YOUR_USERNAME 和 YOUR_REPO_NAME）
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git

# 推送到 GitHub
git branch -M main
git push -u origin main
```

### 第三步：启用 GitHub Pages

1. 在 GitHub 仓库页面，点击 `Settings`（设置）
2. 在左侧菜单中找到 `Pages`
3. 在 `Source` 部分：
    - 选择 `Deploy from a branch`
    - Branch 选择 `main`（或 `master`）
    - Folder 选择 `/ (root)`
4. 点击 `Save`
5. 等待几分钟，GitHub 会显示你的网站地址：
    ```
    https://YOUR_USERNAME.github.io/YOUR_REPO_NAME/
    ```

### 第四步：访问网站

几分钟后，你的朋友就可以通过以下链接访问：

```
https://YOUR_USERNAME.github.io/YOUR_REPO_NAME/
```

## 🔄 更新网站

如果之后修改了代码，只需要：

```bash
cd /Users/aaron/xinxin/弹窗/web_version
git add .
git commit -m "更新内容描述"
git push
```

GitHub Pages 会自动更新（通常需要 1-2 分钟）。

## 📝 注意事项

1. **仓库必须是公开的**：免费版 GitHub Pages 只支持公开仓库
2. **文件位置**：确保 `index.html` 在仓库根目录或 `web_version` 文件夹中
3. **自定义域名**：如果需要使用自己的域名，可以在 Pages 设置中配置

## 🎉 完成！

部署完成后，你的朋友就可以在任何地方通过链接访问这个温馨弹窗网站了！
